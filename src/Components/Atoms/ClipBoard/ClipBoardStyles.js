import styled from '@emotion/styled';

const StyledCopyButton = styled.button`
  width: 'fit-content';
  border-style: 'none';
  font-size: 22px;
  color: green;
`;

const StyledClipBoardWrapper = styled.div``;

export { StyledCopyButton, StyledClipBoardWrapper };
